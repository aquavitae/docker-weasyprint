sudo: required

language: python
python:
  - "3.5"

services:
  - docker

install:
  - docker build -t weasyprint .
  - docker run -d -p 5001:5001 --name weasyprint weasyprint

script:
  - docker ps | grep -q weasyprint
  - python test.py
